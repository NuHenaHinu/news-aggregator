**Installation**
1. Fork and clone repository
2. In the server directory, run npm install
3. Run node server.js to start the server
4. In the client directory, run npm install
5. Run npm run dev to run the client
